/**
 * int _abs -  computes the absolute value of an integer.
 * Return: always 0 its (success)
 */

#include <stdio.h>
#include "main.h"
#include <unistd.h>
#include <stdlib.h>
int _abs(int n)
{
n = abs(n);
return (abs(n));
}
