/**
 * is_prime_number - returns 1 if the num n prime number, otherwise return 0
 * @n : number
 *  Return: return 0 if num n not prime num return 1 if num n is prime num
 */

#include<stdio.h>


int is_prime_number(int n)
{
if (n < 2)
{
return (0);
}
is_prime_number(n % n == 0 && n % 1 == 0);



return (1);

}
