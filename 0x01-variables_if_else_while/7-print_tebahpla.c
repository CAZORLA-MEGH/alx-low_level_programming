#include <stdio.h>

/**
 * main - start point of the program
 * Return: always 0 its (success)
  */
int main(void)
{
char de = 'z';
while(de >= 'a')
{
putchar(ch);
ch--
}
putchar('\n');
return (0);
}
