/**
 * add - function that adds two integers and returns the result.
 * @x : number
 * @z : number
 * Return: x + z
 */
#include <stdio.h>
#include "main.h"
#include <unistd.h>
int add(int x, int z)
{
return (x + z);
}

