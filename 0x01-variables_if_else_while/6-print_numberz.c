#include <stdio.h>
/**
 * main - start point of the program
 * Return: always 0 its (success)
 */
int main(void)
{
int counter = 10;

for (int num = 0; num < counter; num++)
{
putchar(num + '0')
}
putchar('\n')
return (0);
}
