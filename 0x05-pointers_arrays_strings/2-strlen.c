/**
 *_strlen - function that returns the length of a string.
 * @s: contains 16 character
 * @len - Number Character
 * Return: len
*/




#include <stdio.h>
#include <string.h>
int _strlen(char *s)
{
int len;
s = "My first strlen!";
len = strlen(s);
return (len);


}
