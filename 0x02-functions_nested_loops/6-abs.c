/**
 * main - start point of the program
 * Description :
 * Return: always 0 its (success)
 */

#include <stdio.h>
#include "main.h"
#include <unistd.h>
#include <stdlib.h>
int _abs(int n)
{
n = abs(n);
return (abs(n));
}
